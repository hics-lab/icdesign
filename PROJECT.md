# 아날로그 IC 설계 프로젝트 — Folded Cascode OTA & 2‑Stage OPAMP

세종대학교 HICS Lab · 학부 IC 설계
시뮬레이션 도구: **web SPICE 실습 사이트** (ngspice WASM + ETRI 0.5μm PDK)
→ https://hics-lab.github.io/icdesign/practice.html

> 본 과제는 **두 개의 독립 과제**입니다.
> **과제 1 — Folded Cascode OTA**(단일단), **과제 2 — 2‑Stage OPAMP**(Miller 보상).
> 각각 회로 설계 → 사양 측정 → 보고서로 제출합니다.

제출 기한 · 분반 안내: _(교수자 기입)_

---

## 0. 학습 목표

이 프로젝트를 마치면 다음을 할 수 있다.

1. 주어진 토폴로지(folded cascode, 2‑stage)의 각 트랜지스터 역할을 이해하고 **직접 사이징**한다.
2. 모든 소자가 **포화영역**에서 동작하도록 바이어스를 설계·검증한다.
3. **개루프 이득·GBW·위상마진**을 SPICE로 측정한다 (.ac, Bode).
4. **슬루율·정착시간**(.tran), **ICMR·출력 스윙**(.dc), **입력환산 잡음**(.noise)을 측정한다.
5. **파라미터 sweep**으로 설계 트레이드오프(보상용량 vs 위상마진, 전류 vs 슬루율 등)를 정량적으로 탐구한다.
6. 측정값을 목표 사양과 비교하고, 미달 시 **어떤 파라미터를 어떻게 바꿔야 하는지** 근거를 들어 설명한다.

---

## 1. 공통 조건

| 항목 | 값 |
|---|---|
| 공정 | ETRI 0.5μm CMOS, BSIM3v3 (`nmos4` / `pmos4`) |
| 공급전압 | **VDD = 5 V** (단일 전원, GND = 0) |
| 모델 마운트 | netlist 첫 부분에 `.lib "/model.lib" MOS` (M 소자보다 위 줄) |
| 채널길이 | 아날로그용으로 **L ≥ 1u 권장** (최소길이 회피 — 출력저항·정합 유리) |
| 부하용량 C_L | **학생이 정함** (권장 1–5 pF). 모든 AC/슬루율 측정에 동일하게 명시 |

### 1.1 모델의 한계 — 반드시 숙지

이 PDK 임베드 모델은 학부 교육용으로 일부 파라미터가 빠져 있습니다 ([PDK_차이.md](PDK_차이.md) 참고).

- **1/f (flicker) 잡음 파라미터 없음** → `.noise` 결과는 **thermal noise만** 반영. 저주파 입력환산 잡음은 실제보다 크게 낮게 나옴. 보고서에 이 한계를 명시할 것.
- **junction cap 자동계산 안 됨** → MOS에 `AD/AS/PD/PS`를 안 적으면 drain/source 접합 용량이 0으로 처리됨. 고주파/슬루 정확도를 위해 가능하면 명시:
  `M1 d g s b nmos4 W=20u L=1u AD=10p AS=10p PD=21u PS=21u` (AS=AD≈W·0.5u, PS=PD≈2·0.5u+W)
- 동작점·채널전류·게이트 용량(CGDO/CGSO/CGBO)은 **정확**.

### 1.2 SPICE 단위 함정 (감점 단골)

SPICE는 대소문자를 구분하지 않으므로:

| 표기 | 의미 |
|---|---|
| `1M` , `1m` | **1 milli (1e‑3)** — mega 아님! |
| `1Meg` | 1 mega (1e6) |
| `1u` | 1 micro | `1n`,`1p`,`1f` | nano/pico/femto | `1k` | kilo |

→ 주파수 100 메가는 반드시 **`100Meg`**, 1밀리는 `1m`. `100M`은 100밀리로 해석됨.

### 1.3 도구 사용 기초

- 좌측 에디터에 netlist 작성 → 우측 결과 탭(**Tran / DC / AC / Noise**) 선택 → **▶ Run**.
- 한 netlist에 `.tran`, `.dc`, `.ac`, `.noise`를 동시에 둘 수 있고, 각 탭의 Run이 해당 해석만 실행.
- **AC 탭**은 Bode(이득 dB + 위상 deg)를 자동으로 위·아래로 그려줌.
- **파라미터 Sweep**(에디터/로그 사이 바): 소자 1개를 여러 값으로 바꿔 곡선을 겹쳐 그림. 곡선이 많으면 그래프 좌측 **신호 필터**(이름 OR, `@값` OR) 사용.
- 자세한 사용법은 사이트 우상단 **도움말**.

---

## 2. 설계 사양 (목표는 학생이 정한다)

아래는 **권장 범위**입니다. 학생은 각 항목의 **구체적 목표값을 스스로 정하고, 보고서에 그 근거**(응용, 트레이드오프)를 적습니다. 모든 측정은 정한 목표값과 비교합니다.

### 과제 1 — Folded Cascode OTA (단일단)

| 사양 | 권장 범위(최소) | 측정 방법 (§3) |
|---|---|---|
| DC 개루프 이득 A_v | ≥ 60 dB | §3.2 (.ac) |
| 단위이득 대역 GBW | ≥ 5 MHz @ 지정 C_L | §3.2 |
| 위상마진 PM | ≥ 60° | §3.2 |
| 슬루율 SR | ≥ 5 V/μs | §3.3 (.tran) |
| 출력 스윙 | 가능한 넓게 (cascode 제약 명시) | §3.4 |
| 입력 공통모드 범위 ICMR | 범위 보고 | §3.4 |
| 소비전력 | 예산 내 (예: ≤ 2 mW) | §3.5 |
| 입력환산 잡음 | 보고 (1/f 한계 명시) | §3.6 |

### 과제 2 — 2‑Stage OPAMP (Miller 보상)

| 사양 | 권장 범위(최소) | 측정 방법 |
|---|---|---|
| DC 개루프 이득 A_v | ≥ 70 dB | §3.2 |
| GBW | ≥ 5 MHz @ 지정 C_L | §3.2 |
| 위상마진 PM | ≥ 60° (보상으로 확보) | §3.2 |
| 슬루율 SR | ≥ 5 V/μs (≈ I1/C_c) | §3.3 |
| 출력 스윙 | 가능한 rail‑to‑rail에 근접 | §3.4 |
| 소비전력 | 예산 내 | §3.5 |

> 두 회로의 결과를 §6 보고서에서 **비교**하라: 단일단의 고출력저항·고이득 vs 2단의 높은 이득·넓은 스윙·보상 필요성.

---

## 3. 사이트로 사양을 측정하는 법 ★

이 절이 과제의 핵심이다. 각 사양을 **어떤 해석으로, 어떻게** 뽑는지 정리한다.
회로(OTA) 자체는 학생이 작성한다. 아래 **측정 하네스**(테스트벤치)는 본인 회로를 끼워 넣는 틀로 사용하라.

### 3.1 동작점 / 포화 확인 (.dc 단일점 또는 스윕)

목표: 모든 트랜지스터가 **포화** (V_DS > V_GS − V_TH = V_ov) 인지 확인.

방법:
1. 입력을 공통모드 DC(예: 2.5 V)로 두고 출력이 중앙(~2.5 V) 근처에 오도록 바이어스를 잡는다.
2. `.dc` 로 바이어스 전압(또는 V_icm)을 좁게 스윕하고 `.save` 로 주요 노드 전압을 남긴다.
3. 결과 그래프에서 각 노드 전압을 읽어 손으로 V_GS, V_DS 를 계산 → 포화 여부 판정.

```spice
* 동작점 확인 예 (개념 — 본인 노드명으로 교체)
.dc Vcm 2.4 2.6 0.01
.save V(vp) V(vout) V(nodeA)    ; 확인하려는 내부 노드들
```
> 팁: 단일점이 필요하면 `.dc Vdummy 0 0 1` 식보다, 관심 바이어스를 좁게 스윕해 그래프에서 hover 로 값을 읽는 게 편하다.

### 3.2 개루프 이득 · GBW · 위상마진 (.ac, Bode) — 가장 중요

**문제**: 개루프 이득은 매우 커서(수천 배) DC 동작점을 직접 맞추기 어렵다. → **DC에서는 피드백으로 출력을 중앙에 고정**하고 **AC에서는 루프를 끊는** 고전적 하네스를 쓴다.

원리:
- 매우 큰 인덕터 `L`(예: `1T` = 1e12 H): **DC에선 단락**(피드백 성립 → 출력 자동 중앙) / **AC에선 개방**(루프 끊김).
- 매우 큰 커패시터 `C`(예: `1T`): 반전입력을 **AC 가상접지**로 고정 (DC엔 영향 없음).
- 비반전입력에 `AC 1` 인가 → 출력에서 개루프 이득을 그대로 측정.

```spice
* ── 개루프 AC 측정 하네스 (이상적 DC 피드백 / AC 개방) ──
Vdd  vdd 0 DC 5
Vcm  cm  0 DC 2.5            ; 입력 공통모드 (출력이 중앙에 오도록 조정)
Vin  inp cm AC 1            ; 비반전입력 = cm + (AC 1)

* --- 여기에 본인 OTA 연결: 입력 inp, inn / 출력 out / 전원 vdd, 0 ---
*     (M 소자들을 학생이 작성)

Lfb  out inn 1T            ; DC 단락(피드백) / AC 개방
Cfb  inn 0  1T            ; 반전입력 AC 가상접지
CL   out 0  2p            ; 부하용량 (사양에 맞게)

.ac dec 20 1 100Meg
.end
```

**읽는 법** (AC 탭 Bode 뷰, 신호 `v(out)`):
- **DC 이득 A_v** = 저주파 평탄부 |H| 값 (dB).
- **GBW** = |H| 가 **0 dB**를 지나는 주파수.
- **위상마진 PM** = (GBW 주파수에서의 위상) **+ 180°**.
  - 이 하네스는 비반전 측정이라 **저주파 위상이 ≈ 0°** 여야 정상. 만약 ±180°에서 시작하면 입력 극성(inp↔inn)을 바꿔라.
  - 예: GBW에서 위상이 −120° 이면 PM = 180° + (−120°) = **60°**.

> C_L 을 바꾸면 GBW·PM 이 변한다. **사양표에 사용한 C_L 을 반드시 명시**하라.

### 3.3 슬루율 · 정착시간 (.tran)

방법: **단위이득 버퍼**(출력→반전입력 직결)로 구성하고 입력에 큰 **계단(step)** 을 준다.

```spice
* ── 슬루율 측정 (단위이득 버퍼 + 계단 입력) ──
Vdd vdd 0 DC 5
Vin inp 0 PULSE(1.5 3.5 1u 1n 1n 10u 20u)   ; 1.5V→3.5V 계단
* --- 본인 OTA: inp(+), inn(−), out, vdd --- (inn 은 out 에 직결: 단위이득)
* 예) 출력을 반전입력에 연결:  (네트명으로 inn = out)
CL  out 0 2p
.tran 10n 15u
.end
```
**읽는 법**: 출력 파형의 **가장 가파른 구간 기울기** = SR (V/μs). 상승/하강 각각 측정. 최종값의 ±0.1%(또는 1%)에 드는 시각 = **정착시간**. (그래프 확대/hover 활용)

> 주의: `inn` 과 `out` 을 같은 노드로 두면 단위이득. 슬루는 큰 신호 현상이므로 작은 스텝(소신호)이 아니라 **충분히 큰 스텝**을 줄 것.

### 3.4 ICMR · 출력 스윙 (.dc)

- **ICMR**: 단위이득 버퍼에서 `Vin` 을 0→5 V 스윕(.dc)하고 `v(out)` 을 본다. 출력이 입력을 잘 따라가는(기울기≈1) 구간이 ICMR. 양 끝에서 벗어나는 지점이 한계.
  ```spice
  .dc Vin 0 5 0.01
  ```
- **출력 스윙**: 개루프(또는 높은 이득) 상태에서 출력이 선형성을 유지하는 V_out 의 최대/최소. 입력 차동을 미세 스윕하거나, ICMR 측정의 선형구간 끝에서 읽는다. cascode/2단 구조에 따른 차이를 비교·서술.

### 3.5 소비전력 (.dc 또는 동작점)

`.dc` 결과에서 **`i(Vdd)`** (VDD 전원으로 흐르는 전류)를 읽어 **P = VDD × |I_VDD|**. 정지전류 기준으로 계산.
> AC/Tran 입력란과 무관하게, 전원 전류는 결과의 전류(I) 뷰에서 `i(Vdd)` 신호로 확인.

### 3.6 입력환산 잡음 (.noise)

```spice
* 입력 source Vin 기준 입력환산 잡음
.noise V(out) Vin dec 20 1 100Meg
```
- 입력 source에 `AC 1` 필요(없으면 ngspice 오류). 출력 노드·입력 source 명을 본인 회로에 맞게.
- Noise 탭에서 대역 적분으로 RMS 잡음 자동 계산.
- **⚠ 1/f 미포함**: §1.1 — 저주파 잡음이 실제보다 낮음. 보고서에 한계 명시.

### 3.7 파라미터 Sweep로 설계 탐구 (필수 활용)

에디터/로그 사이 **⟲ Sweep** 바: 소자 1개를 여러 값으로 바꿔 곡선을 겹쳐 그린다. 예:
- **보상용량 C_c vs 위상마진**: AC 탭에서 `Cc` 를 `1p, 2p, 5p` 로 sweep → PM 변화 관찰 (2단 보상 설계의 핵심).
- **입력쌍 W vs 이득/GBW**: gm 변화 → GBW.
- **바이어스 전류 vs 슬루율·전력**: tail 전류원 W 를 sweep → SR·전력 트레이드오프.

각 과제 보고서에 **최소 1개 이상의 sweep 연구**를 포함하고, 곡선으로 결론을 뒷받침하라.

---

## 4. 과제 1 — Folded Cascode OTA

### 4.1 토폴로지 (학생이 사이징·작성)

핵심 구성요소:
- **차동 입력쌍** (NMOS 또는 PMOS — 선택·근거 제시): 입력 트랜컨덕턴스 gm 결정.
- **folding 노드 + cascode 디바이스**: 입력단 전류를 접어(fold) 출력으로 전달, 출력 임피던스를 cascode로 높여 **고이득** 확보.
- **cascode 전류원 부하**: 출력저항 r_out 극대화.
- **바이어스 회로**: cascode 게이트 바이어스 전압들(여러 개) 생성. 간단히 전압원으로 줘도 되나, 근거를 적을 것.

> 단일단이므로 **출력 노드의 지배극점 + C_L** 로 자체 보상되는 경우가 많다. 별도 보상소자 없이 PM≥60°가 나오는지 확인하고, 부족하면 C_L/전류로 조정.

### 4.2 설계 절차 (권장)

1. **전류 예산**: 전력 사양에서 총 전류 → 각 가지 전류 배분.
2. **입력쌍 사이징**: 목표 GBW ≈ gm_in / (2π·C_L) 에서 필요한 gm_in 계산 → §3 갤러리의 Id–Vgs(.dc)와 **W sweep**로 W 결정 (overdrive V_ov ≈ 0.15–0.25 V 권장).
3. **cascode 사이징**: 포화 유지하며 출력 스윙을 최대화하도록 V_ov 작게.
4. **바이어스 전압 결정**: 모든 소자 포화(§3.1)로 검증.
5. **측정**: §3.2 이득/GBW/PM → §3.3 SR → §3.4 ICMR/스윙 → §3.5 전력 → §3.6 잡음.
6. 미달 항목을 **sweep**으로 조정(§3.7).

---

## 5. 과제 2 — 2‑Stage OPAMP (Miller 보상)

### 5.1 토폴로지 (학생이 사이징·작성)

- **1단**: 차동입력쌍 + 능동부하(전류미러) → 1차 이득.
- **2단**: 공통소스 증폭(+ 전류원 부하) → 추가 이득 + **넓은 출력 스윙**.
- **Miller 보상**: 1단 출력↔2단 출력 사이에 **C_c**(필요시 **널링 저항 R_z** 직렬) → 극점 분리(pole splitting)로 PM 확보, RHP zero 제거.
- **바이어스**: 1·2단 전류원 바이어스.

### 5.2 설계 절차 (권장)

1. **C_c 선택**: PM 확보를 위해 보통 C_c ≳ 0.22·C_L 부터 시작.
2. **슬루율**: SR ≈ I_1 / C_c (1단 tail 전류 I_1). 슬루 사양에서 I_1 하한 결정.
3. **GBW**: GBW ≈ gm_in / (2π·C_c) → 필요한 gm_in → 입력쌍 W (.dc/W sweep).
4. **2단**: 출력 스윙·구동력 위해 2단 트랜지스터·전류 사이징.
5. **RHP zero**: 필요하면 R_z ≈ 1/gm_2 직렬로 보상(또는 제거). 효과를 sweep로 확인.
6. **측정 + sweep**: §3 전부. 특히 **C_c sweep으로 PM 최적화**(§3.7) 필수 포함.

> 2단은 보상이 핵심이다. **보상 전/후의 Bode(이득·위상)**를 비교해 pole splitting 효과를 보여라.

---

## 6. 제출물 · 평가

### 6.1 제출물 (과제별로 각각)

1. **netlist 파일** (회로 + 각 측정 테스트벤치). 사이트 **JSON export** 또는 `.cir` 텍스트.
2. **사양 비교표**: 본인이 정한 목표값 vs 측정값 (달성/미달 표시).
3. **Bode 그림**(이득·위상, GBW·PM 표시), **슬루 파형**, **ICMR/스윙**, **잡음** 캡처.
4. **파라미터 sweep 연구** 최소 1건(곡선 + 해석).
5. **보고서**(§6.3).

### 6.2 평가 루브릭 (예시 — 교수자 조정)

| 항목 | 배점 |
|---|---|
| 회로 동작·전 소자 포화 검증 | 20 |
| 이득/GBW/PM 측정·목표 달성 | 25 |
| 슬루율·ICMR·스윙·전력·잡음 측정 | 20 |
| 파라미터 sweep 기반 설계 근거 | 15 |
| 보고서 완성도·고찰(모델 한계 포함) | 20 |

### 6.3 보고서 템플릿

1. 설계 목표(정한 사양 + 근거)
2. 회로도 + 소자 사이징 표(W/L, 전류, V_ov)
3. 동작점·포화 검증
4. 측정 결과(각 사양, 그림 포함)
5. 파라미터 sweep 연구 및 트레이드오프 분석
6. 목표 대비 결과 표 + 미달 항목 개선 방안
7. 고찰: 모델 한계(1/f 잡음 등), 단일단 vs 2단 비교
8. netlist 부록

---

## 7. 자주 막히는 곳 (FAQ)

- **결과에 `boltz, FALSE` 등 이상한 신호** → 분석이 실패한 것(존재하지 않는 sweep 소스/노드 등). 에디터·노드명 확인. (사이트가 "시뮬 실패"로 처리)
- **AC 이득이 1(0 dB) 근처로 평탄** → 개루프 하네스의 L/C 연결 확인. inn이 AC 가상접지인지, out→inn 피드백 L 이 큰지.
- **위상마진이 음수/이상** → 저주파 위상이 0°가 아니면 입력 극성(inp↔inn) 교체.
- **노이즈가 평탄(1/f 없음)** → §1.1, 정상. 한계 명시.
- **`.noise` 오류 "input source has no AC value"** → 입력 source에 `AC 1` 추가.
- **고주파에서 cap 효과가 약함** → `AD/AS/PD/PS` 미기입(§1.1). 명시하면 개선.
- **단위 혼동** → §1.2. 100메가 = `100Meg`.

문제가 지속되면 사이트 **Log 탭**의 ngspice 메시지를 확인하라. 도구 사용법은 사이트 **도움말** 모달 참고.

---

_본 문서는 web SPICE 실습 사이트(ETRI 0.5μm PDK)에 맞춰 작성되었습니다. 측정 하네스의 구체 수치(L/C, C_L, 스텝 등)는 회로 규모에 따라 조정하세요._
